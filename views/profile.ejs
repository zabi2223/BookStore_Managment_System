<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <title>Profile | Apni Book</title>
    <style>
        /* Reset & Base */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: "Poppins", sans-serif;
        }

        body {
            background: #f0f2f7;
            color: #333;
            line-height: 1.6;
        }

        /* Back Button */
        .back-btn {
            position: fixed;
            top: 20px;
            left: 20px;
            background: #6C63FF;
            color: #fff;
            padding: 10px 16px;
            border-radius: 8px;
            font-size: 14px;
            font-weight: 600;
            text-decoration: none;
            display: inline-block;
            transition: all 0.3s ease;
            z-index: 1000;
            box-shadow: 0 6px 16px rgba(108, 99, 255, 0.25);
        }

        .back-btn:hover {
            background: #574fd1;
            transform: translateY(-2px);
        }

        /* Profile Container */
        .profile-container {
            max-width: 520px;
            margin: 80px auto 50px;
            background: #fff;
            padding: 30px 25px;
            border-radius: 18px;
            box-shadow: 0 10px 25px rgba(0, 0, 0, 0.12);
            text-align: center;
            position: relative;
        }

        /* Profile Pic */
        .profile-pic {
            position: relative;
            width: 140px;
            height: 140px;
            margin: 0 auto 25px;
        }

        .profile-pic img {
            width: 140px;
            height: 140px;
            border-radius: 50%;
            object-fit: cover;
            border: 4px solid #6C63FF;
            box-shadow: 0 8px 18px rgba(108, 99, 255, 0.25);
        }

        /* Edit Icon */
        .edit-icon {
            position: absolute;
            bottom: 5px;
            right: 5px;
            width: 38px;
            height: 38px;
            background: #6C63FF;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            color: #fff;
            cursor: pointer;
            font-size: 16px;
            border: 2px solid #fff;
            transition: all 0.25s ease;
            user-select: none;
        }

        .edit-icon:hover {
            background: #574fd1;
            transform: scale(1.08);
        }

        /* Form */
        form {
            text-align: left;
            margin-top: 10px;
        }

        form input {
            width: 100%;
            padding: 12px 14px;
            margin-bottom: 15px;
            border-radius: 10px;
            border: 1px solid #ddd;
            font-size: 14px;
            outline: none;
            transition: all 0.3s ease;
        }

        form input:focus {
            border-color: #6C63FF;
            box-shadow: 0 0 6px rgba(108, 99, 255, 0.25);
        }

        form input[readonly] {
            background: #f6f6f6;
            cursor: not-allowed;
        }

        /* Section Title */
        form h3 {
            margin: 15px 0 10px;
            font-size: 18px;
            color: #6C63FF;
            text-align: left;
        }

        /* Notification styles */
        .notification {
            position: fixed;
            top: 20px;
            right: 20px;
            background: #6C63FF;
            color: #fff;
            padding: 15px 20px;
            border-radius: 10px;
            box-shadow: 0 8px 20px rgba(0, 0, 0, 0.2);
            font-size: 14px;
            font-weight: 500;
            opacity: 0;
            transform: translateY(-20px);
            animation: slideIn 0.5s forwards, fadeOut 0.5s 3.5s forwards;
            z-index: 1000;
        }

        @keyframes slideIn {
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        @keyframes fadeOut {
            to {
                opacity: 0;
                transform: translateY(-20px);
            }
        }

        /* Button */
        form button {
            width: 100%;
            padding: 12px;
            border: none;
            border-radius: 12px;
            background: #6C63FF;
            color: #fff;
            font-size: 15px;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s ease;
        }

        form button:hover {
            background: #574fd1;
            transform: translateY(-1px);
            box-shadow: 0 10px 18px rgba(108, 99, 255, 0.25);
        }

        /* Responsive */
        @media (max-width: 600px) {
            .profile-container {
                margin: 25px 15px;
                padding: 25px 18px;
            }

            .profile-pic {
                width: 120px;
                height: 120px;
            }

            .profile-pic img {
                width: 120px;
                height: 120px;
            }
        }

        #passwordMessage {
            font-size: 14px;
            margin-top: -10px;
            margin-bottom: 10px;
            font-weight: 500;
            transition: 0.3s ease;
        }
    </style>
</head>

<body>
    <!-- Back Button (link version) -->
    <a href="/home" class="back-btn">← Back</a>
    <div class="profile-container">

        <form action="/updateProfile" method="POST" enctype="multipart/form-data">
            <div class="profile-pic">
                <img src="<%= user.pic%>" alt="Profile" id="profileImg">
                <label for="newPic" class="edit-icon">✏️</label>
                <input type="file" id="newPic" name="pic" accept="image/*" hidden>
            </div>
            <input type="text" name="name" value="<%= user.name %>" placeholder="Your Name">
            <input type="email" name="email" value="<%= user.email %>" readonly>

            <h3>Change Password</h3>
            <input type="password" id="oldPassword" name="oldPassword" placeholder="Old Password">
            <input type="password" id="newPassword" name="newPassword" placeholder="New Password">
            <input type="password" id="confirmPassword" name="confirmPassword" placeholder="Confirm Password">
            <p id="passwordMessage"></p>
            <p style="text-align: right; margin-top: -15px; margin-bottom: 20px; font-size: 14px;">
                <a href="/forgot-password" style="color: #6C63FF; text-decoration: none;">Forgot Password?</a>
            </p>
            <button type="submit">Update Profile</button>
        </form>
    </div>

    <% if (typeof message !=='undefined' ) { %>
        <div id="notification" class="notification">
            <%= message %>
        </div>
        <% } %>

            <script src="/js/profilescript.js"></script>
</body>

</html>