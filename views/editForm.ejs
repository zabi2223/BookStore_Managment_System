<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <title>Edit Book</title>
    <style>
        /* Reset & Base */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Poppins', sans-serif;
        }

        body {
            background: #f0f2f7;
            color: #333;
            line-height: 1.6;
        }

        a {
            text-decoration: none;
        }

        /* Main Wrapper */
        .page-wrapper {
            padding: 40px;
            display: flex;
            justify-content: center;
            align-items: flex-start;
        }

        /* Form Card */
        .form-container {
            width: 100%;
            max-width: 520px;
            background: #fff;
            padding: 30px;
            border-radius: 15px;
            box-shadow: 0 10px 20px rgba(0, 0, 0, 0.1);
        }

        .form-container h2 {
            text-align: center;
            margin-bottom: 25px;
            color: #6C63FF;
            font-size: 24px;
            font-weight: 600;
        }

        /* Inputs */
        /* Inputs (ONLY AddBook form) */
        .form-container form input {
            width: 100%;
            padding: 12px 14px;
            margin-bottom: 15px;
            border: 1px solid #ddd;
            border-radius: 10px;
            font-size: 14px;
            transition: all 0.3s ease;
        }

        .form-container form input:focus {
            border-color: #6C63FF;
            box-shadow: 0 0 6px rgba(108, 99, 255, 0.3);
            outline: none;
        }

        /* Button (ONLY AddBook form) */
        .form-container form button {
            width: 100%;
            padding: 12px;
            border: none;
            border-radius: 10px;
            cursor: pointer;
            font-weight: 600;
            font-size: 15px;
            background: #6C63FF;
            color: #fff;
            transition: all 0.3s ease;
        }

        .form-container form button:hover {
            background: #574fd1;
        }


        /* Extra Back Button (Optional) */
        .back-btn {
            display: inline-block;
            margin-bottom: 18px;
            background: #eee;
            padding: 10px 14px;
            border-radius: 10px;
            font-weight: 500;
            color: #333;
            transition: all 0.3s ease;
        }

        .back-btn:hover {
            background: #ddd;
        }

        /* Responsive */
        @media (max-width: 600px) {
            .page-wrapper {
                padding: 20px;
            }

            .form-container {
                padding: 20px;
            }
        }

        .back-btn {
            background: #6C63FF;
            color: #fff;
            padding: 10px 16px;
            border-radius: 8px;
            font-size: 14px;
            font-weight: 600;
            text-decoration: none;
            display: inline-block;
            transition: all 0.3s ease;
            z-index: 1000;
            box-shadow: 0 6px 16px rgba(108, 99, 255, 0.25);
        }

        .back-btn:hover {
            background: #574fd1;
            transform: translateY(-2px);
        }

        /* Notification styles */
        .notification {
            position: fixed;
            top: 20px;
            right: 20px;
            background: #6C63FF;
            color: #fff;
            padding: 15px 20px;
            border-radius: 10px;
            box-shadow: 0 8px 20px rgba(0, 0, 0, 0.2);
            font-size: 14px;
            font-weight: 500;
            opacity: 0;
            transform: translateY(-20px);
            animation: slideIn 0.5s forwards, fadeOut 0.5s 3.5s forwards;
            z-index: 1000;
        }

        /* Slide in from top */
        @keyframes slideIn {
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        /* Fade out after delay */
        @keyframes fadeOut {
            to {
                opacity: 0;
                transform: translateY(-20px);
            }
        }
    </style>
</head>

<body>
    <%- include('partails/header') %>
        <div class="page-wrapper">
            <div class="form-container">
                <a href="/home?page=<%= page %>" class="back-btn">← Back</a>
                <h2>Edit Book</h2>
                <form action="/editBook/<%= existBook._id %>?page=<%= page %>" method="POST">
                    <input type="text" name="title" value="<%= existBook.title %>" placeholder="Enter title" required>
                    <input type="text" name="author" value="<%= existBook.author %>" placeholder="Enter author"
                        required>
                    <input type="number" name="price" value="<%= existBook.price %>" min="1" placeholder="Enter price"
                        required>
                    <input type="text" name="isbn" value="<%= existBook.isbn %>" placeholder="Enter ISBN" required>
                    <input type="date" name="publishedDate"
                        value="<%= existBook.publishedDate.toISOString().slice(0,10) %>" required>
                    <button type="submit">Update</button>
                </form>

            </div>
        </div>
        <% if (typeof message !=='undefined' ) { %>
            <div id="notification" class="notification">
                <%= message %>
            </div>
            <% } %>
                <script src="/js/script.js"></script>
</body>

</html>