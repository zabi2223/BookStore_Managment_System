<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <title>Home | Apni Book</title>
    <style>
        /* Reset & Base */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Poppins', sans-serif;
        }

        body {
            background: #f0f2f7;
            color: #333;
            line-height: 1.6;
        }

        a {
            text-decoration: none;
        }

        /* Main Container */
        .main-container {
            display: flex;
            padding: 40px;
            gap: 30px;
        }

        /* Filters Sidebar */
        .filters {
            background: #fff;
            padding: 20px;
            border-radius: 15px;
            width: 250px;
            height: 250px;
            box-shadow: 0 10px 20px rgba(0, 0, 0, 0.1);
            flex-shrink: 0;
        }

        .filters h3 {
            margin-bottom: 10px;
            color: #6C63FF;
            font-size: 20px;
            text-align: center;
        }

        .filters label {
            display: block;
            margin: 10px 0 5px;
            font-weight: 500;
        }

        .filters input {
            width: 100%;
            padding: 8px 10px;
            margin-bottom: 15px;
            border: 1px solid #ddd;
            border-radius: 8px;
            font-size: 14px;
            transition: all 0.3s ease;
        }

        .filters input:focus {
            border-color: #6C63FF;
            box-shadow: 0 0 6px rgba(108, 99, 255, 0.3);
            outline: none;
        }

        .filters button {
            padding: 10px;
            margin-right: 5px;
            border: none;
            border-radius: 8px;
            cursor: pointer;
            font-weight: 500;
            transition: all 0.3s ease;
        }

        .filters button[type="submit"] {
            background: #6C63FF;
            color: #fff;
        }

        .filters button[type="submit"]:hover {
            background: #574fd1;
        }

        .filters button[type="reset"] {
            background: #eee;
        }

        .filters button[type="reset"]:hover {
            background: #ddd;
        }

        /* Book List Section */
        .book-list {
            flex: 1;
            background: #fff;
            padding: 25px;
            border-radius: 15px;
            box-shadow: 0 10px 20px rgba(0, 0, 0, 0.1);
        }

        .top-bar {
            display: flex;
            justify-content: flex-end;
            margin-bottom: 20px;
        }

        .add-btn {
            background: #6C63FF;
            color: #fff;
            padding: 10px 15px;
            border-radius: 8px;
            font-weight: 500;
            transition: all 0.3s ease;
        }

        .add-btn:hover {
            background: #574fd1;
        }

        /* Table Styles */
        table {
            width: 100%;
            border-collapse: collapse;
        }

        thead {
            background: #6C63FF;
            color: #fff;
        }

        thead th {
            padding: 12px;
            text-align: left;
            font-weight: 500;
        }

        tbody tr {
            border-bottom: 1px solid #eee;
            transition: background 0.2s ease;
        }

        tbody tr:hover {
            background: #f5f5ff;
        }

        tbody td {
            padding: 12px;
        }

        .edit-btn {
            background: #4CAF50;
            color: #fff;
            padding: 6px 10px;
            border-radius: 6px;
            font-size: 14px;
            margin-right: 5px;
            transition: all 0.3s ease;
        }

        .edit-btn:hover {
            background: #3e8e41;
        }

        .delete-btn {
            background: #FF4C4C;
            color: #fff;
            padding: 6px 10px;
            border-radius: 6px;
            font-size: 14px;
            border: none;
            transition: all 0.3s ease;
            cursor: pointer;
        }

        .delete-btn:hover {
            background: #d43f3f;
        }

        /* Pagination */
        .pagination {
            margin-top: 20px;
            display: flex;
            justify-content: center;
            gap: 8px;
        }

        .pagination a {
            display: inline-block;
            padding: 8px 12px;
            background: #eee;
            color: #333;
            border-radius: 6px;
            font-weight: 500;
            transition: all 0.3s ease;
        }

        .pagination a.active {
            background: #6C63FF;
            color: #fff;
        }

        .pagination a:hover {
            background: #574fd1;
            color: #fff;
        }

        /* Responsive */
        @media (max-width: 900px) {
            .main-container {
                flex-direction: column;
                padding: 20px;
            }

            .filters {
                width: 100%;
                margin-bottom: 20px;
            }


        }

        /* Notification styles */
        .notification {
            position: fixed;
            top: 20px;
            right: 20px;
            background: #6C63FF;
            color: #fff;
            padding: 15px 20px;
            border-radius: 10px;
            box-shadow: 0 8px 20px rgba(0, 0, 0, 0.2);
            font-size: 14px;
            font-weight: 500;
            opacity: 0;
            transform: translateY(-20px);
            animation: slideIn 0.5s forwards, fadeOut 0.5s 3.5s forwards;
            z-index: 1000;
        }

        /* Slide in from top */
        @keyframes slideIn {
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        /* Fade out after delay */
        @keyframes fadeOut {
            to {
                opacity: 0;
                transform: translateY(-20px);
            }
        }
    </style>

</head>

<body>
    <%- include('partails/header') %>

        <div class="main-container">
            <aside class="filters">
                <h3>Filter Books</h3>
                <form id="filterForm" action="/filter" method="POST">
                    <label>Price Range:</label>
                    <input type="number" placeholder="Min" value="<%= minPrice %>" min="1" name="minPrice">
                    <input type="number" placeholder="Max" value="<%= maxPrice %>" min="1" name="maxPrice">
                    <button type="submit">Apply</button>
                    <button type="reset" id="resetFilter">Reset</button>
                </form>
            </aside>

            <section class="book-list">
                <div class="top-bar">
                    <a href="/addBook" class="add-btn">+ Add Book</a>
                </div>

                <table>
                    <thead>
                        <tr>
                            <th>Book ID</th>
                            <th>Title</th>
                            <th>Author</th>
                            <th>Published</th>
                            <th>Price</th>
                            <th>Action</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% books.forEach(book=> { %>
                            <tr>
                                <td>
                                    <%= book.isbn %>
                                </td>
                                <td>
                                    <%= book.title %>
                                </td>
                                <td>
                                    <%= book.author %>
                                </td>
                                <td>
                                    <%= book.publishedDate.toDateString() %>
                                </td>
                                <td>
                                    <%= book.price %>
                                </td>
                                <td>
                                    <a href="/editBook/<%= book._id %>?page=<%= currentPage %>"
                                        class="edit-btn">Edit</a>
                                    <form action="/deleteBook/<%= book._id %>?page=<%= currentPage %>" method="POST"
                                        style="display:inline;">
                                        <button type="submit" class="delete-btn">Delete</button>
                                    </form>
                                </td>
                            </tr>
                            <% }) %>
                    </tbody>
                </table>
                <div class="pagination">
                    <% for (let i=1; i <=totalPages; i++) { %>
                        <a href="/home?page=<%= i %>" class="<%= currentPage === i ? 'active' : '' %>">
                            <%= i %>
                        </a>
                        <% } %>
                </div>
            </section>
        </div>

        <%- include('partails/footer') %>
            <% if (typeof message !=='undefined' ) { %>
                <div id="notification" class="notification">
                    <%= message %>
                </div>
                <% } %>

                    <script src="/js/script.js"></script>
</body>

</html>